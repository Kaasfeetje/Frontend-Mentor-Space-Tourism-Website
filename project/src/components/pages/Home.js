import React from "react";
import Header from "../Header/Header";

const Home = () => {
    return (
        <div className="home w-screen h-screen">
            <Header />
        </div>
    );
};

export default Home;
